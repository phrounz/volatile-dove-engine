App_VS2008GL
	Should work just by opening the solution and compiling+running it (with F5).

App_VS2013_DX_Desktop
	Should work just by opening the solution and compiling+running it (with F5).

App_VS2013_DX_WinStore
	Requires to compile+run the solution, then stop debugging after it crashes, 
	then copy WorkDir\default_font.png into 
	App_VS2013_DX_Store\Debug[or Release]\App_VS2013_DX_Store\AppX, 
	then run the solution again.
